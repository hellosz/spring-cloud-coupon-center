server:
  port: 20001
  error:
    include-message: always

spring:
  datasource:
    username: fabulous
    password: Tn87941597.
    url: jdbc:mysql://8.135.120.218:3306/geekbang_coupon?autoReconnect=true&useUnicode=true&characterEncoding=UTF-8&serverTimezone=UTC&useSSL=false&allowPublicKeyRetrieval=true&zeroDateTimeBehavior=convertToNull
    driver-class-name: com.mysql.cj.jdbc.Driver
    type: com.zaxxer.hikari.HikariDataSource
    # 连接池
    hikari:
      pool-name: coupon-template-serv-hikariCP
      auto-commit: true
      connection-timeout: 5000
      idle-timeout: 30000
      maximum-pool-size: 5
      minimum-idle: 2
      max-lifetime: 60000
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        format_sql: true
        show_sql: true
    open-in-view: false
  cloud:
    nacos:
      # nacos 服务发现
      discovery:
        enabled: true
        server-addr: 127.0.0.1:8848
        namespace: 947a7b68-c711-470b-bc3c-9a337001dc16
        group: COUPON
        metadata:
          mydata: coupon-customer-serv


logging:
  level:
    com.ralph.coupon: debug
    org.hibernate.type.descriptor.sql: trace
    com.ralph.coupon.customer.feign: debug
feign:
  client:
    config:
      # 全局默认
      default:
        # 网络连接超时
        connect-timeout: 1000
        # 读超时
        read-timeout: 5000
      # 指定服务
      coupon-template-serv:
        # 网络连接超时
        connect-timeout: 1000
        # 读超时
        read-timeout: 5000
  # hystrix 服务降级
  circuitbreaker:
    enabled: true